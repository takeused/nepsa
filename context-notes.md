# 다음 작업 시작점 (2026-09-06)

최신 상태와 남은 작업은 [이어받기 메모](docs/handoff-2026-09-06.md)를 먼저 읽으세요. 아래 내용은 누적 기록이며 일부 과거 파일 구조와 테스트 수는 현재와 다릅니다.

# 외부 자료 조사 (2026-09-06)

전체 기록은 [docs/nepsa-external-sources-2026-09-06.md](docs/nepsa-external-sources-2026-09-06.md).

NEPSA를 다룬 자료 두 건을 새로 찾아 원문까지 읽었다. **NEPSA는 고정 산식이 아니라 기관마다 항목·척도·등급 경계를 다시 짜는 템플릿 계열이다.**

- **KISTEP(2017) 사물인터넷 정부투자 우선순위 연구** (TRKO201800036073) — 약어를 `NExt Project Selection Analysis`로 푼다. 기대수익 8항목·위험성 8항목을 1/4/7/10 네 눈금으로 채점하고 R-R Chart 4분면(Desirable → Challenge → Insurance → Never)에 배치한다. 가중치는 신성장기술/원천기술로 나뉜다. ScienceON이 공개한 표 이미지(214~217쪽)를 직접 읽었다.
- **국토교통과학기술진흥원(2021) 어린이 통학버스 기획** (CODIL OTKCRK210954) — 지표 6개(KEIT 12지표 중 정성지표만)·5점 척도·S~D 5등급. **S를 고위험·고성과로 잡아 KEIT 기업주관형과 반대다.** 그림 출처를 KISTEP(2018)로 밝혀 위 자료가 상위 출처임을 확인해 준다.

**소수점 값의 정체를 알아냈다.** 통학버스 보고서는 "평가위원 6명"이 채점했고 결과가 전부 1/6 단위다(70.17·49.33·70.83…). 2013년 KEIT 예시의 70.83·26.67도 같은 성질이므로, "1~5 정수로는 어떤 환산식으로도 안 나온다"는 기존 관찰의 원인은 **산식이 아니라 평가자 집계**일 가능성이 높다. 다만 2013년 원문에 평가자 수가 없어 환산식을 특정하지는 못한다 — 앱의 (n−1)÷4×100은 그대로 구현 가정이다.

**추가 추적 결과(같은 날 2차).** 남겨 뒀던 실마리 두 개를 모두 닫았다.

- **KEIT(2011) IT R&D 기획연구** (TRKO201100005978, 주관 KEIT·연구책임자 하상태) 157쪽에 「개발형 과제의 NEPSA 검증 결과 및 우선순위 부여 예시」 그림이 있다. **2013년보다 2년 앞선 KEIT 자체 용례**이고, 앞 쪽이 TRL 정의·평가지표라 개발형 과제는 TRL+NEPSA를 함께 썼다. 격자를 픽셀로 재니 위험 경계는 25·50·75로 같지만 **기대성과 경계가 50·70·80·90**이고 위험 25~75가 한 칸으로 묶여 있다. 즉 **KEIT 내부에서도 2011→2013 사이에 격자를 다시 그렸다.** 균등 4×4는 2013년판의 선택이다. 예시 우선순위표는 같은 칸의 두 과제에 같은 순위를 준다(앱의 동률 처리와 같은 성격). 다만 한 점(사)이 이 규칙을 벗어나 있어 순위 규칙 전체는 재구성하지 못했다.
- **KISTEP(2010) 국가 R&D 투자 우선순위 설정 방법론 연구** — PDF 237쪽 전문을 PyMuPDF로 추출해 전수 검색한 결과 **NEPSA 언급이 없다.** 이 실마리는 닫는다.

계산 로직은 하나도 바꾸지 않았다. 다른 판본을 같은 화면에 섞으면 어느 쪽이 원문 기준인지 흐려지기 때문이다. 반영은 출처 탭 카드 2개, 개요 탭 약어 병기, 예시 재현 한계 설명 보강까지다.

여전히 못 찾은 것: 2013년 100점 환산 산식, 12영역 경계값 정의, IP 부상도 기준표의 2013년 원본, 공개 구현.

# 이어받은 작업 검토 (2026-09-06)

이전 회차 커밋 `ae6f347`을 검토했다. 테스트 127개·lint·tsc·build는 통과했고, 계산 로직(기술격차 십진 비교, 영역 경계, 상세 CSV 열 수)을 별도 격자 검산으로 재확인해 오류가 없었다. 사용자에게 보이는 문구·문서에서 다음 4건을 고쳤다.

- 분석 탭이 `String(e)`로 예외를 표시해 화면에 `Error:` 접두사가 나왔다. `say()`로 통일해 포트폴리오 탭과 같은 형식으로 맞췄다.
- 이력이 20회로 찬 상태에서 저장하면 `parseHistory`의 "백업 형식을 확인하세요" 메시지가 떠 원인을 오도했다. `record()`에서 한도를 먼저 확인해 삭제 안내를 준다.
- 이력 백업 버튼의 `serializeHistory`가 try 밖에 있어 예외가 화면으로 새어 나갈 수 있었다.
- 범위 오류 안내와 이력 변경 내역이 `marketSize`·`ip:source` 같은 내부 키를 그대로 노출했다. `rawLabels`와 `fieldLabel()`로 입력 화면 이름을 쓴다.
- README의 구조 블록이 `lib/workspace.ts`·`lib/analysis.ts`·`app/evaluation.tsx`·`app/analysis-panel.tsx` 추가 이전 상태였다.

고치지 않고 남긴 관찰:

- `raw.world`가 `null`이면 시장규모가 채점되지 않는다(`undefined`는 국내시장으로 처리). 현재 UI 경로로는 `null`이 생기지 않아 방어 코드로 두었다.
- 시나리오 편집기의 삭제 버튼은 `disabled={scenario}`라 `onDelete`가 실행되지 않는다. 시나리오를 닫으려면 새 분석을 시작해야 한다.

# 최신 검토 메모 (2026-09-05)

아래는 누적 작업 기록이며, 현재 사양은 README의 “이어받기 검토 및 보완”과 코드를 우선합니다.

- 100점 환산식은 원문 공식으로 확인되지 않은 구현 가정이다. ÷5 방식도 위험 20점에서 S 영역에 도달하므로, 과거 기록의 “도달 불가능” 주장은 철회한다.
- CODIL 본문의 “높은 수준”만으로 정수 사사오입이 입증되지는 않는다. 반올림과 구간 경계는 현재 구현 가정으로 유지하며 화면에 표시한다.
- 원문 예시의 지표 원점수·평가자 집계 방식은 알 수 없다. 원문 10개 종합점수로 검증하는 것은 등급·순위이고 환산식 자체가 아니다.
- 부분 입력한 IP 수치에 예전 수동 점수를 사용하는 동작을 제거했다. 일부만 입력하면 미완료이며 모든 수치를 비우면 수동 점수를 사용한다.
- 저장 전 유효성 검사, 구버전 재계산 안내, 적용 모델 식별자, 이전 원본 다운로드를 추가했다.
- 범위 오류·IP 전환·복원 회귀 테스트와 지표별 기여점수 표시를 추가했다.

---

# NEPSA 구현 컨텍스트 노트

출처: `../1383551433622750798153.pdf` — KEIT PD Issue Report 13-10 (2013), PDF 20~25쪽(인쇄 100~105쪽).
추출 이미지: `../tmp/pdfs/page-18.png` ~ `page-25.png`.

## 스택

vinext 1.0.0-beta.5 (Next.js 호환 레이어) + React 19.2 + Cloudflare Vite 플러그인 + shadcn/base-ui.
`npm run dev` → localhost:3000, `npm run build` → `dist/server/wrangler.json`.

## 구조

- `lib/nepsa.ts` — 도메인 로직 전부. 12개 지표 정의, 자동 채점, 축 점수, 등급 판정, 정렬, 백업 검증.
- `app/page.tsx` — 단일 클라이언트 컴포넌트. Matrix / Editor / Methodology 서브컴포넌트.
- `app/globals.css` — 디자인 토큰 + 전체 커스텀 클래스.

## 원문 대조로 확인한 사항

- 12개 지표와 가중치: 기대성과 6개(15/10/15/25/15/20), 위험 6개(20/15/15/25/10/15). 각 축 합계 100.
- 매트릭스 12개 영역 배치와 우선순위 `S>A1>A2>B1>B2>B3>B4>C1>C2>C3>D1>D2` — PDF 24쪽 확인.
- 원천기술형(제한없음) 도전적 R&D 영역: 기대성과 ≥ 60, 위험 ≥ 40. 축 눈금은 기대성과 60/70/80/90/100, 위험 40/55/70/85/100으로 재척도. `classify()`의 `(ret-60)/40*100`, `(risk-40)/60*100`이 이 눈금과 일치함을 검산했다.
- 25쪽 예시 10개 과제(AAA~JJJ)는 종합점수만 제공되고 지표별 원점수는 없다. 그래서 `mode:'direct'` 표본으로 넣었다.

## 원문에 없어 구현 가정으로 처리한 것

앱의 "평가 기준·출처" 탭에 그대로 노출된다. 임의로 정하지 말고 이 목록을 유지할 것.

1. **경계값** — 기대성과 25/50/75는 위쪽 구간, 위험 25/50/75는 오른쪽 구간에 포함.
2. **100점 환산** — 원문에 산식이 없어 두 방식을 설정으로 뒀다. 기본은 `÷5×100`, 대안은 `(n-1)÷4×100`.
3. **정렬** — 대등급 우선(문서 예시 기준)과 세부 영역 우선(본문 순서) 둘 다 설정으로 제공.
4. **IP 부상도** — 원문에 기준표가 없다. 자동 채점하지 않고 1~5점 수동 입력. CODIL 보고서 표 3-10의 4항목 평균 방식을 발견했으나 2013년 공식 기준이라는 근거가 없어 채택하지 않았다.
5. **원문 오탈자 해석** — 세계시장 2점 구간은 20억~50억 달러, 사업화 비용 4점 구간 상한은 200억원으로 읽었다.
6. **동률** — 등급·기대성과까지 같으면 과제명 → id 순. 평가 우열이 아니다.

## 이번 회차(2026-09-05 이어받기)에 한 일

작업이 중단된 지점은 **CSS**였다. `page.tsx`가 쓰는 클래스 55개 중 12개만 정의돼 있어 레이아웃이 없는 상태였다.

- `app/globals.css`에 누락 클래스 53개 전부 작성 (163줄). 1180px / 680px 두 브레이크포인트 포함.
- **하이드레이션 불일치 수정** — SVG `<title>`에 텍스트 자식을 여러 개 넣으면 React 19 SSR이 내용을 비운 `<title></title>`을 내보낸다(문서 metadata로 오인). 단일 템플릿 문자열로 바꿔 해결. 서버 HTML에 툴팁 텍스트가 실제로 들어가는 것까지 curl로 확인했다.

## 테스트

`npm test` (vitest). `lib/nepsa.test.ts` 57개.

앱 출력이 아니라 **원문 표를 기준으로** 단언한다. 25쪽 예시 10개 과제의 기대성과·위험·등급·우선순위, 21~22쪽 정량지표 점수기준의 모든 구간 경계, 지표 가중치, 23~24쪽 12개 영역 배치가 대상이다.

`vitest.config.ts`를 따로 둔 이유 — 앱의 `vite.config.ts`에는 Cloudflare/vinext 플러그인이 걸려 있어 테스트가 이를 끌어오면 느리고 불안정하다. `lib/nepsa.ts`는 import가 없는 순수 모듈이라 node 환경만으로 충분하다.

**테스트 작성 중 걸린 함정** — 정량지표 4개(market·profit·gap·resources)는 `scores`에 직접 넣어도 반영되지 않는다. `scoresFor()`가 `{...p.scores, ...autoScores(p.raw)}` 순서라 `raw`가 비어 있으면 `autoScores`가 돌려주는 `null`이 수동 점수를 덮어쓴다. 그래서 테스트 헬퍼 `rawFor(n)`으로 원시 입력을 만들고, 이 헬퍼가 실제로 12개 지표를 n점으로 채우는지 확인하는 전제 테스트를 따로 뒀다.

**뮤테이션 검증** — 테스트가 헛돌지 않는지 확인하려고 `nepsa.ts`에 결함 6종(가중치 변경, 구간 경계값 변경, 부등호 방향, 원천기술형 하한, 정렬 방향, 사사오입→버림)을 하나씩 심어 봤다. 전부 테스트가 잡았다. 원본은 되돌렸다.

## 지표 점수 정수 검사

`validateImport`가 `n<1||n>5`만 보고 정수 여부는 확인하지 않아 `2.5` 같은 값이 통과했다. 테스트를 쓰다 발견했고, 원문 기준표가 1~5점 정수이므로 `!isNumber(n)` 을 `!Number.isInteger(n)` 으로 바꿨다. `Number.isInteger`는 NaN·Infinity·비수치를 모두 거르므로 `isNumber`를 겹쳐 쓸 필요가 없다(`isNumber`는 파일 내 다른 8곳에서 계속 쓰인다).

UI의 Select는 1~5 정수만 만들어내므로 정상 사용 경로에서는 영향이 없다. 손으로 고친 백업 파일이나 손상된 localStorage만 거부된다. 거부됐을 때의 동작은 실제로 확인했다 — 경고를 띄우고 자동 저장을 멈추며, **기존 저장값을 덮어쓰지 않는다**. 사용자가 원본을 잃지 않고 복구할 수 있다.

## 다크 모드

`.dark` 토큰은 있었지만 아무도 그 클래스를 붙이지 않아 죽은 코드였고, 커스텀 클래스는 hex 58개를 하드코딩하고 있었다. 세 갈래로 정리했다.

**1. 색 토큰화** — `globals.css` 상단 한 블록에 색을 모으고 나머지 규칙은 전부 `var()`만 쓴다. 표면(`--panel`, `--panel-subtle`), 텍스트(`--label`, `--soft`), 상태(`--danger`, `--warning-*`, `--ok-dot`), 상단바(`--topbar-*`) 등 역할 단위로 묶었다. 비슷한 회색 열몇 개는 `--muted-foreground` 하나로 합쳤다.

**2. 등급색을 JS에서 CSS로** — `gradeColors`가 hex를 들고 있어 테마 전환이 닿지 않았다. 값을 `var(--grade-s)` 형태로 바꾸고 배경용 `gradeTints`를 분리했다(기존에는 hex에 `'15'`를 문자열로 붙여 알파를 만들고 있어서 `var()`와 함께 쓸 수 없었다). 주의할 점 하나 — **SVG는 `fill="var(--x)"` 같은 presentation attribute로는 변수가 해석되지 않는다.** `style={{fill:...}}`로 넘겨야 한다. 매트릭스 사각형·점·라벨을 전부 이 방식으로 바꿨다.

**3. 토글** — 테마의 단일 출처는 `<html>`의 `dark` 클래스다. React 상태로 복제하지 않는다.
- 최초값은 `layout.tsx`의 인라인 스크립트가 첫 페인트 전에 정한다. 저장된 선택이 없으면 OS 설정(`prefers-color-scheme`)을 따른다. `<html suppressHydrationWarning>`이 필요하다.
- 아이콘 전환도 CSS(`.on-light` / `.on-dark`)가 맡는다. 처음에는 `useState`+`useEffect`로 DOM 클래스를 읽어 상태에 복제했는데, oxlint의 `EffectSetState`에 걸렸고 마운트 시 불필요한 재렌더도 생겼다. 상태를 없애니 린트 오류·재렌더·하이드레이션 불일치가 한꺼번에 사라졌다.

다크에서는 등급색을 밝은 쪽으로 다시 잡고(`#146c60` → `#3fbfa8`), 점 안 숫자 색을 `--dot-ink`로 뒤집었으며, 영역 채움 불투명도도 `--region-fill`로 올렸다(.075 → .16). 라이트 팔레트를 그대로 어두운 배경에 얹으면 대비가 무너지기 때문이다.

## 참조 조사 (2026-09-05)

GitHub 검색 API로 `NEPSA`, `R&D portfolio management`, `project prioritization matrix`, `KEIT`, `국가R&D` 등을 조회했다. 이 방법론을 구현한 저장소는 없다. `NEPSA` 상위 결과는 전부 동명이의(Brazilian 알고리즘 학습 사이트, 광고 SDK, 이커머스 등 최대 2스타)이고, `KEIT`는 Keithley·携帯(keitai) 같은 부분일치였다. 참조해서 보완할 만한 선행 구현이 없어 아무것도 반영하지 않았다. 앱의 "평가 기준·출처" 탭에도 이 조사 결과를 반영해 뒀다.

## 100점 환산식을 (n−1)÷4×100 하나로 고정

가상 포트폴리오 14개를 만들어 실제로 돌려보다가 발견했다.

**÷5×100은 산출 범위가 0~100이 아니라 20~100이다.** 지표 최저가 1점이라 가중평균이 1 아래로 못 내려간다. 그런데 매트릭스 경계는 25/50/75다. 그 결과 **S 영역(위험<25)과 최하단 행(기대성과<25)이 사실상 도달 불가능**하다. 14개를 돌렸을 때 S 칸이 끝까지 비어 있었고, 같은 데이터를 (n−1)÷4로 바꾸자 한 과제가 A1 → S로 올라갔다.

처음에는 두 방식을 설정으로 남기고 기본값만 바꿨다. 그러나 원문 예시로 어느 쪽도 검증되지 않는다는 것이 확인된 이상 선택지를 남겨 두면 "둘 중 하나가 원문에 맞다"는 잘못된 인상을 준다. 그래서 `(n−1)÷4×100` 하나로 고정하고 `Settings`에서 `normalization`을 제거했다. 폐기한 이유는 앱의 평가 기준 탭·README·이 노트에 남겼다.

**호환성** — 구버전 백업에는 `normalization` 필드가 들어 있다. `validateImport`가 이 필드를 검사하지 않고 무시하도록 두어 예전 백업도 그대로 열린다. `five`였든 `zero`였든 이제 같은 산식으로 계산되므로 점수가 달라질 수 있는데, 이는 의도한 결과다. 회귀 테스트로 잠갔다.

산식은 `axisScore()` 한 줄이고 `normalizationLabel` 상수가 표시·CSV 기록을 함께 담당한다. 다른 산식으로 바꾸려면 이 두 곳만 고치면 된다.

**원문 예시로는 어느 쪽도 검증할 수 없다.** 25쪽 예시 점수의 산출 가능성을 따져 봤다. 1~5 정수 지표로 계산하면 ÷5는 0.2 배수, (n−1)÷4는 0.25 배수만 나올 수 있다. 그런데 `70.83`과 `26.67`은 어느 배수에도 해당하지 않는다(÷5로 불가능한 값 4개, (n−1)÷4로 2개). 즉 예시 점수는 12개 지표 정수 채점의 결과가 아니다. `26.67`(=80/3)은 평가위원 3인 평균 같은 다른 과정을 시사한다. 예시 과제를 `mode:'direct'`로 넣은 기존 판단이 결과적으로 옳았다.

## 예제 데이터

`docs/example-portfolio.json` — 12개 지표를 모두 채운 가상 과제 14개(기업주관 11 · 원천기술 3). 등급이 A1~C3로 퍼지도록 구성했고 정성평가 분포 경고가 뜨는 사례도 포함했다.

수치는 전부 지어낸 것이다. 각 과제 근거란에도 그렇게 적어 뒀다. 실제 시장 데이터로 오인해 인용하는 일이 없도록 README에도 경고를 달았다.

만드는 과정에서 도구가 내 편향을 잡았다. 처음에는 유망 기술만 골라 넣어 14개가 전부 B등급으로 나왔고, 정성평가 분포 검증이 5개 지표에서 60% 초과를 경고했다. 금형·편광필름 같은 평범한 과제를 섞고 나서야 등급이 퍼졌다. 검증 기능이 의도대로 작동한다는 방증이다.

## IP 부상도 자동 채점 (기준표 출처 확인)

이전 회차까지 "원문에 기준표가 없어 수동 입력"으로 남겨 뒀던 항목이다. 이번에 원문을 끝까지 뒤져 보고 외부 출처를 실제로 열어 확인한 뒤 자동 채점으로 바꿨다.

**1. 원문에는 정말 없다.** PDF 20쪽은 IP 부상도를 정량 지표로 분류하고 점수 칸에 "점수기준표 참조"라고 적어 두었다. 그런데 참고 1-1~1-3 표(21~22쪽)에는 기술격차·영업이익률·시장규모/성장률·사업화 요구자원만 있고 IP 표가 없다. 문서 27쪽 전체를 텍스트로 훑어 "부상도"·"점수기준" 키워드를 검색해도 20~22쪽 외에는 나오지 않는다. 원문 자체의 누락이다.

**2. 외부 출처를 직접 열어 확인했다.** 이전 회차에 열람 실패했던 CODIL 보고서(OTKCRK230019)를 받아 표 3-10을 확인했다. 표 제목이 **"IP부상도 판단결과 및 항목별 판단기준표"**이고 표 안 머리글이 **"NEPSA 중 특허평가지표"**다. NEPSA의 지표라고 자기 기술하고 있다는 점이 중요하다.

산식 — 네 항목(출원증가율 / 국내출원인 출원건수 증가율 / 최근구간 점유율 / 특허 시장확보력)을 모두 같은 구간표로 채점하고 평균을 낸다.

| 구간 | 1 | 2 | 3 | 4 | 5 |
| --- | --- | --- | --- | --- | --- |
| 공통 | 0% 미만 | 0~20% | 20~60% | 60~80% | 80% 이상 |

**3. 보고서 본문으로 교차검증했다.** 본문에 실측치가 서술돼 있다 — 출원증가율 72%, 국내출원인 124%, 최근구간 점유율 38%, 시장확보력 48%. 구간표를 적용하면 4·5·3·3점이고 평균 3.75다. 본문은 이를 "높은 수준의 IP 부상도"라고 적었는데 이는 4점(높음)이므로 **사사오입이 맞다.** 별개로 본문이 점유율 38%를 "보통수준"이라 한 것도 3점과 일치한다. 두 서술이 독립적으로 산식을 뒷받침한다. 이 검증은 회귀 테스트에 그대로 박아 뒀다.

**남는 한계** — CODIL 보고서는 2023년 자료다. 2013년 KEIT 문서가 의도한 기준표와 같다는 보장은 없다. 그래서 앱의 평가 기준 탭과 README에 "다른 지표와 근거의 무게가 다르다"고 명시했다. 다른 지표는 2013년 원문 직접 인용인 반면 이것만 후속 자료 추정이다.

정리 하나 — 환산식을 고정하면서 `axisScore`·`evaluate`의 `settings` 인자가 죽은 코드가 되어 있었다(린트가 잡아 줬다). 함께 제거했다. `rank`만 `sorting` 때문에 계속 받는다.

**하위 호환** — 특허분석 네 값이 모두 있을 때만 자동 채점하고, 없으면 기존 `scores.ip`를 그대로 쓴다(`scoresFor`에서 `auto.ip ?? p.scores.ip`). 기준표 도입 이전 백업이 그대로 열리고, 특허분석 수치 없이 보고서 점수만 아는 경우도 계속 지원된다. UI도 수치가 비어 있을 때만 직접 입력 셀렉트를 노출한다.

## 우선순위 정렬을 12개 영역 순서로 고정

원문에 근거가 없다고 보고 설정으로 열어 두었던 마지막 항목이다. 23·24쪽을 정확히 다시 읽으니 근거가 있었다.

- **기준 3**(23쪽) — "총 5개 등급(S›A›B›C›D)으로 구분하되, 동일 등급 과제의 경우 기대성과가 높은 과제 우선"
- **3-2)·3-3)**(23·24쪽) — 기업주관형·원천기술형 **양쪽 모두**에 `S›A(A1›A2)›B(B1›B2›B3›B4)›C(C1›C2›C3)›D(D1›D2)`

앞선 회차에 "대등급 정렬은 문서 예시 기준, 영역 정렬은 본문 순서"라고 적어 둔 것은 두 조항을 대립 관계로 잘못 본 것이다. 실제로는 충돌하지 않는다. 영역 순서에서 대등급 문자만 뽑으면 `S A A B B B B C C C D D`라 기준 3의 순서가 그대로 보존되고, 3-2)는 거기에 영역 단위 세분을 더한 더 구체적인 규정이다. 그래서 영역 순서를 따르고 동일 영역 안에서 기준 3대로 기대성과 내림차순을 적용한다.

**더 나아가 두 해석은 어떤 입력에서도 결과가 같다.** 같은 등급 안에서 각 영역이 가질 수 있는 기대성과 구간이 서로 겹치지 않기 때문이다.

| 등급 | 영역별 기대성과 구간 |
| --- | --- |
| A | A1 75~100 · A2 50~75 |
| B | B1 75~100 · B2 50~75 · B3 25~50 · B4 0~25 |
| C | C1 50~75 · C2 25~50 · C3 0~25 |
| D | D1 25~50 · D2 0~25 |

즉 영역 순서가 이미 기대성과 내림차순이다. 그동안 노출해 온 "우선순위 정렬" 설정은 **실제로는 아무것도 바꾸지 않는 선택지**였다. 매트릭스 전 좌표를 훑어 이 불변식을 확인했고 회귀 테스트로 잠갔다.

**설정 개념 자체를 제거했다.** 환산식에 이어 정렬까지 고정되면서 `Settings` 타입에 남는 필드가 없어졌다. 타입·상태·백업 필드·Methodology 설정 UI를 모두 걷어냈다. 백업 JSON은 이제 `{version, projects}`뿐이다. 구버전 백업의 `settings`는 검사 없이 무시하므로 그대로 열린다(문자열이 들어 있어도 통과). CSV에는 `normalizationLabel`·`sortingLabel` 상수로 적용된 규칙을 기록해 파일만 봐도 산출 근거를 알 수 있게 했다.

## 라이선스 결정

코드는 MIT(`LICENSE`). 저작권자는 GitHub 핸들 `takeused`, 연도는 2026으로 적었다. 실명이나 법인명을 쓸지는 사용자 확인이 필요하다.

경계를 나눈 이유 — 저장소에는 성격이 다른 두 가지가 섞여 있다. 직접 작성한 코드·예제 데이터·스크린샷은 MIT로 풀어도 되지만, 12개 지표명과 설명 문구, 가중치, 기준표 구간값, 12개 영역 배치, 25쪽 예시 점수는 KEIT 보고서에서 가져온 것이다. 수치 자체는 사실에 가깝더라도 설명 문구와 선택·배열까지 MIT로 재배포 허용한다고 선언하면 권리 범위를 넘어선다. 그래서 README 라이선스 절에서 둘을 명시적으로 분리했다.

확인 실패 사항 — 참조 PDF 27쪽 전체에서 저작권·공공누리·ISSN 표기를 검색했으나 없었다. 발췌본이라 판권지가 빠졌을 가능성이 크다. KEIT는 준정부기관이라 공공누리 대상일 수 있지만 확인되지 않았으므로 "확인 못 했다"고 그대로 적고 재출판·상업적 배포 전 직접 문의를 권고했다. 임의로 공공누리라고 단정하지 않았다.

제3자 — `components/ui/` 60개 파일은 shadcn/ui에서 복사한 것이다. `node_modules`의 `shadcn`·`@shadcn/react` package.json에서 `license: MIT`와 LICENSE.md 존재를 실제로 확인했다(기억이 아니라 확인). 고지 의무는 없지만 파일 수가 저장소의 다수를 차지하므로 출처를 밝혔다.

## 인쇄 / PDF 레이아웃

`@media print` 블록으로 처리했다. 세 가지가 핵심이었다.

**1. 종이 전용 팔레트를 따로 뒀다.** 화면 라이트 팔레트를 재사용하지 않는다. 종이는 순백 배경·검은 글자가 기준이고 등급색도 어두운 쪽이 잉크로 잘 나오기 때문이다. `:root,:root.dark` 양쪽에 선언해 **다크 테마로 보고 있어도 인쇄는 항상 흰 종이 기준**으로 나간다. JS로 인쇄 직전 dark 클래스를 떼는 방법도 있었지만, `beforeprint` 이벤트에 의존하지 않는 CSS 해법이 더 견고하다고 봤다.

**2. `<details>` 안의 내용은 인쇄되지 않는다.** 지표별 평가 근거와 정성평가 분포 경고가 모두 details 안에 있었다. 닫힌 details의 내용은 CSS로 안정적으로 펼칠 수 없다(브라우저가 shadow DOM 슬롯으로 숨긴다). 그래서 인쇄 전용 문단(`.print-note`, `.distribution-print`)을 따로 렌더하고 화면에서는 숨긴다. **평가 근거가 빠진 보고서는 점수의 산출 이유를 알 수 없어 쓸모가 크게 떨어지므로** 이 처리는 필수였다.

**3. 검증은 실제 인쇄 출력으로 했다.** CDP `Page.printToPDF`로 A4 PDF를 뽑아 pypdf로 페이지별 텍스트를 추출했다. 상단바·버튼·푸터 등 조작 요소 10종이 빠졌는지, 표제·매트릭스·표·근거가 들어갔는지를 문자열로 대조했다. 눈으로만 봤다면 근거 누락을 놓쳤을 것이다. 별도로 `Emulation.setEmulatedMedia({media:'print'})` + 스크린샷으로 색과 레이아웃도 확인했다.

예제 데이터의 결함도 이 과정에서 드러났다. `notes.summary`에 근거를 넣어 뒀는데 **summary는 종합점수 직접 입력 모드에서만 화면에 표시된다.** 지표 평가 과제에 넣으면 앱 어디에서도 보이지 않는 죽은 데이터였다. 지표별 notes로 옮겼다.

## lint 정리

`npm run lint` 오류 0개.

vendored 코드(`components/ui/**`, `hooks/use-mobile.ts`)는 shadcn/ui에서 복사한 것이라 상류의 스타일 선택이다. 우리가 고치면 재동기화 때 충돌하고, 규칙 제안 중에는 해당 컴포넌트에 부적절한 것도 있다(예: 툴바 그룹의 `role="group"`을 `fieldset`으로 바꾸라는 제안). **`ignorePatterns`로 통째로 제외하지 않고 `overrides`로 문제되는 규칙만 껐다.** 진짜 버그는 계속 잡히게 두기 위해서다.

우리 코드(`app/page.tsx`) 7건 중 4건은 실제로 고쳤다 — label과 Select를 `htmlFor`/`id`로 연결(2건), CSV 셀 함수의 `unknown` 타입을 실제 넘기는 타입으로 좁힘, `role="status"`를 `<output>`으로 교체. 나머지 3건은 정당한 패턴이라 사유를 적은 `oxlint-disable-next-line`으로 처리했다. SVG 안에는 `<button>`을 중첩할 수 없고, localStorage는 SSR 렌더 중에 읽을 수 없어 마운트 후 동기화가 불가피하다.

## 검증 상태

`npm test` 57개 통과 · `npx tsc --noEmit` 통과 · `npm run build` 통과 · 라이트/다크 양쪽 렌더 확인 · 브라우저에서 하이드레이션 오류 없음 · 12개 지표 폼·매트릭스·평가 기준 탭 렌더 확인.

`npm run lint` 오류 0개.

## NEPSA 개요 탭 (2026-09-06)

"NEPSA가 뭔지 모르겠다"는 요청으로 `app/overview.tsx`를 추가했다. 첫 번째 탭이다.

**약어 풀이를 원문에서 찾았다.** 지금까지 이 저장소 어디에도 NEPSA가 무엇의 약자인지 적혀 있지 않았다. PDF 18쪽(인쇄 098쪽)에 `NEPSA(Nest yEar Projects Selection Analysis)`로 실려 있다. `Nest yEar`는 문맥상 `Next Year`의 오타로 보이지만 **원문 표기를 그대로 옮기고 추정임을 밝혔다.** 임의로 고쳐 쓰면 출처를 확인하려는 사람이 원문에서 같은 문자열을 못 찾는다.

**지표 묶음도 원문에서 가져왔다.** 20쪽에 기대성과가 Output(1차 성과) → Outcome(2차 성과) → Impact로, 위험이 Technology Risk → Market Risk로 묶여 있다. 이 묶음은 `lib/nepsa.ts`의 `criteria`에 없던 정보라 `overview.tsx`의 로컬 맵으로 뒀다. 도메인 로직이 쓰지 않는 표시 전용 정보를 순수 모듈에 넣지 않기 위해서다.

**`regionGrid`를 export로 뽑았다.** 개요의 미니 매트릭스와 `classify()`가 같은 4x4 배치를 봐야 하는데, 배열을 복사해 두면 한쪽만 고쳐질 수 있다. `classify()` 안의 리터럴을 `lib/nepsa.ts`의 `regionGrid`로 올리고 양쪽이 참조한다.

**설명 수위.** 대상 독자가 방법론을 모르는 사람이라 등산 코스 비유, "잘 되면 얼마나 좋은가 / 얼마나 어려운가" 같은 평문을 앞에 두고, 산식과 근거는 기존 '평가 기준·출처' 탭으로 넘겼다. 두 탭이 같은 내용을 반복하지 않게 개요에서는 결론만 말하고 근거는 링크로 넘긴다.

### 모바일 가로 넘침 — 개요 탭이 드러낸 기존 버그

개요 표(5열)를 넣자 375px 폭에서 문서 `scrollWidth`가 1358px까지 벌어졌다. 추적해 보니 원인은 표가 아니라 `.workspace`였다.

`main.workbench`는 세로 flex이고 `.workspace`에 `margin:auto`가 걸려 있다. **가로 margin이 auto면 flex 자식의 stretch가 꺼지고 shrink-to-fit으로 크기가 정해져, 내용이 넓으면 컨테이너보다 커진다.** 그래서 안쪽에 넓은 표가 하나만 있어도 페이지 전체가 밀렸다. 개요 탭 이전에도 `keepMounted`인 분석 탭 때문에 375px에서 556px까지 넘치고 있었다 — 개요 탭은 이걸 눈에 띄게 키웠을 뿐이다.

`.workspace`에 `width:100%`를 줘서 화면 폭에 고정했다. 넓은 표는 이미 있는 방식대로(`.chart-scroll`, `.comparison-scroll`) 각자의 `overflow-x:auto` 컨테이너 안에서 가로 스크롤한다. 개요 표도 `.overview-scroll`로 감쌌다. 탭이 4개가 되면서 탭 목록도 375px에 안 들어가 좁은 화면에서만 가로 스크롤을 걸었다.

결과: 375px 초과분 1358px → 388px. 남은 13px은 `.page-heading .actions`의 버튼 3개다. 개요 탭과 무관한 기존 문제라 손대지 않고 `checklist.md`에 남겼다.

## 남은 것

`checklist.md` 참고.
